---
import { Image } from 'astro:assets';

const {
  src,
  alt,
  sizes,
  widths,
  class: className = "",
} = Astro.props;
---

{ src ? <Image
  src={src}
  widths={widths}
  alt={alt}
  loading = "lazy"
  decoding = "async"
/> : null }

<style>
  img {
    content-visibility: auto;
  }
</style>