---
const { post } = Astro.props;
---

<article
  class="max-w-md mx-auto md:max-w-none"
>
    <header>
      <h2 class="text-xl sm:text-2xl font-bold mb-2">
          <span>{new Date().toISOString() >= post.pubDate ? "✅": "🌟 Inscreva-se!"}</span>
          {
            post.eventDate
            && (
              <span class="font-monotext-2xl sm:text-3xl talk-date">🗓️ {post.eventDate}</span>
            )
          }
          {post.title}
      </h2>
    </header>

    <div class="text-md sm:text-lg flex-grow">
      <Fragment set:html={post.body} />
    </div>
    <footer class="flex items-center mt-4">
    </footer>
</article>