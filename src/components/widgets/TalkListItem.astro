---
import { render } from 'astro:content';

const { talk } = Astro.props;
const { Content } = await render(talk);
---

<article
  class="max-w-md mx-auto md:max-w-none"
>
    <header>
      <h2 class="text-xl sm:text-2xl font-bold mb-2">
          <span>{new Date().toISOString() >= talk.data.pubDate.toISOString() ? "✅": "🌟 Inscreva-se!"}</span>
          {
            talk.data.eventDate
            && (
              <span class="font-monotext-2xl sm:text-3xl talk-date">🗓️ {talk.data.eventDate}</span>
            )
          }
          {talk.data.title}
      </h2>
    </header>

    <div class="text-md sm:text-lg flex-grow prose prose-slate dark:prose-invert max-w-none talk-content">
      <Content />
    </div>
    <footer class="flex items-center mt-4">
    </footer>
</article>