version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Astro 7 requires Node >= 22.12
        - nvm install 22 && nvm use 22
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .astro/**/*
