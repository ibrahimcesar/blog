version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Astro 7 requires Node >= 22.12
        - nvm install 22 && nvm use 22
        # rehype-mermaid's pre-mermaid strategy never launches a browser
        - export PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .astro/**/*
